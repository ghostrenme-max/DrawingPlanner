/**
 * @typedef {{
 *   workTimer: boolean
 *   selfFeedback: boolean
 *   gallery: boolean
 *   goalScreen: boolean
 *   imageUploadSlot: boolean
 * }} AppFeatures
 */

/** @type {AppFeatures} */
export const DEFAULT_APP_FEATURES = {
  workTimer: true,
  selfFeedback: true,
  gallery: true,
  goalScreen: true,
  imageUploadSlot: true,
}
